#!/bin/sh
set -x

mkdir $DESTDIR/app
cp -r $SRCDIR/* $DESTDIR/app
cd $DESTDIR/app

git clean -f

bundle config set development true
bundle config set without develpoment

BUNDLE_PATH="$DESTDIR/opt/bundle" bundle install

bundle config set path /opt/bundle
rm -rf /opt/bundle/ruby/*/cache

